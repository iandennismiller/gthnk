[questions]
home_directory.question = What is the path of your home directory?
home_directory.required = True
home_directory.default = /Users/idm

secret_key.question = What is the secret key? (python -c 'import os; print(repr(os.urandom(24)))' | pbcopy)
secret_key.required = True

hash_salt.question = What is the hash_salt? (python -c 'import string as s, random as r; print repr("".join(r.choice(s.letters+s.digits) for _ in range(16)))' | pbcopy)
hash_salt.required = True